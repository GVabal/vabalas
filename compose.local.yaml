volumes:
  pg_data:
    driver: local

services:
  postgres:
    restart: always
    image: postgres:17
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_DB: pgdb
      POSTGRES_USER: sa
      POSTGRES_PASSWORD: changeme
