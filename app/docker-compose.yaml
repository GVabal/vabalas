services:
  reverse-proxy:
    image: traefik:v3.1
    command:
      - "--providers.docker"
      - "--providers.docker.exposedbydefault=false"
      - "--entryPoints.websecure.address=:443"
      - "--certificatesresolvers.app.acme.tlschallenge=true"
      - "--certificatesresolvers.app.acme.email=vabalas@vabalas.my.id"
      - "--certificatesresolvers.app.acme.storage=/letsencrypt/acme.json"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - letsencrypt:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock
  app:
    image: app:prod
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app.rule=Host(`vabalas.my.id`)"
      - "traefik.http.routers.app.entrypoints=websecure"
      - "traefik.http.routers.app.tls.certresolver=myresolver"
    environment:
      PORT: "80"
    deploy:
      mode: replicated
      replicas: 1
    restart: always
    healthcheck:
      test: "curl --fail --silent 127.0.0.1/actuator/health | grep UP || exit 1"
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
volumes:
  letsencrypt:
